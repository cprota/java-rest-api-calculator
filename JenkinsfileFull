pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
				git 'https://github.com/cprota/java-rest-api-calculator.git'
				sh './mvnw clean compile' 
            }
        }
        stage('Test') {
            steps {
				sh './mvnw test'	
            }
			post {
				always {
				    junit '**/target/surefire-reports/TEST-*.xml'
                     // Junit mettiamo altri info jjj  jjj  iii iii jjjj ii ii  iiii oooo
				}	
            }			
        }
        stage ('Publish'){
            steps{
                sh './mvnw package'
                // Install the Maven version configured as "M3" and add it to the path uuu
            }
			post {
				success {
				    archiveArtifacts 'target/*.jar'
				}	
            }			
        }
    }
}